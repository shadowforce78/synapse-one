import React from 'react';

const EditorHeader = () => {
  return (
    <header className="editor-header">
      <h1>Markdown Editor</h1>
      <div className="editor-controls">
        {/* Add any controls like save, export, etc. here */}
      </div>
    </header>
  );
};

export default EditorHeader;