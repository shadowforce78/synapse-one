import React from 'react';

const PreviewHeader = () => {
  return (
    <header className="preview-header">
      <h2>Markdown Preview</h2>
      <p>See your Markdown content rendered live!</p>
    </header>
  );
};

export default PreviewHeader;